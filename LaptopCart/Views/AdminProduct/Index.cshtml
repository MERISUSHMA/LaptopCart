@model IEnumerable<Product>

<partial name="_NotificationMessage" />



@*@if(TempData["success"]!=null)
{
    <h2>@TempData["success"]</h2>
}*@

    <div class="row align-items-center mb-3">
        <div class="col-md-2">
            <h2 class="text-secondary">Products List</h2>
        </div>
        <div class="col-md-10 text-end">
        <a asp-action="Create" class="btn btn-dark text-white"><i class="bi bi-plus-circle"></i>Add Product</a>
        </div>
    </div>

    <table class="table table-bordered table-striped">
        <thead>
            <tr>
                <th>Product ID</th>
                <th>Product Name</th>
                <th>Description</th>
                <th>Price</th>
                <th>Image</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach(var item in Model)
            {
            <tr>
                <td>@item.Id</td>
                <td>@item.Name</td>
                <td>@item.Description</td>
                <td>₹@item.Price</td>
               <td>
                   <img src="@item.ImagePath" width="100" height="100" />
               </td>
               <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning"><i class="bi bi-pencil-fill"></i>Edit</a>
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-warning"><i class="bi bi-archive-fill"></i>Delete</a>
               </td>
            </tr>
        }
        </tbody>
    </table>